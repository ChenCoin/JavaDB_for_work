该程序由Java语言编写，需要安装才能运行，软件安装后通常会自动打开，如果没能自动打开，可以在 所有程序 找到它。

该软件安装在C:\Users\“用户名”\AppData\Local中，不可以在软件运行时卸载软件，正常卸载的话，软件可以被卸载干净。

启动程序后，需选择附带的数据库文件夹JavaDB。

软件中的打印功能没有在真实的打印机上测试，只测试通过windows自带的Windows to PDF输出PDF文件。 